import routes from "./routes";
import { permissions, rolePermissions, roles } from "./rbacConfig";

const config = {
  routes,
  permissions,
  rolePermissions,
  roles,
};

export default config;
